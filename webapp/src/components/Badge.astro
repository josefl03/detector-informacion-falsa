<div style=`--color: ${Astro.props.color || 'var(--accent-color-light)'};` id={Astro.props.id} hidden={Astro.props.hidden}>
    {Astro.props.icon && (<span>{Astro.props.icon}</span>)}
    <p id=`${Astro.props.id}-text`>
        <slot/>
    </p>
</div>

<style>
    div{
        padding: 4px 10px;
        margin: 0;
        width: fit-content;

        border-radius: 999px;
        border: 1px solid var(--accent-color-light);

        font-size: 0.9rem;
        font-weight: 300;

        border-color: var(--color);
        color: var(--color);
    }

    div[hidden] {
        opacity: 0;
    }

    span{
        margin-right: 1px;
    }

    p{
        display: inline-block;

        padding: 0;
        margin: 0;

        filter: brightness(60%);
    }
</style>