<div {...Astro.props}>
    <slot />
</div>

<style>
    div{
        display: flex;
        flex-direction: column;
        align-items: start;
        justify-content: start;

        width: 50%;
        height: 100%;

        gap: var(--gap-size);flex: 0 0 auto;
    }
</style>